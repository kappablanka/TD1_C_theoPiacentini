#include <stdlib.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
    int age = 0;
    printf("quel est votre age ? : ");
    scanf("%d", &age);
    printf("votre age est : %d", age);
    return 0;
}
